#include <iostream>
#include "RunProtal.h"

int main(int argc, char *argv[]) {
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);


    protal::Run(argc, argv);
//
//
//    protal::RunOld(argc, argv);
//    protal::Test(argc, argv);

    return 0;
}
